# ---- Local plain PostgreSQL (tests/000 harness mode) ----
PGHOST=localhost
PGPORT=5433
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=boutiqueos_test

# ---- Supabase DEV project (only after backend gate is passed) ----
SUPABASE_PROJECT_REF=
SUPABASE_DB_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
